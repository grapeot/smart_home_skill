# Hue bridge
HUE_BRIDGE_IP=192.0.2.10

# Hue light name
HUE_LIGHT_NAME=Your Light Name

# Server port
PORT=7999

# Bind hosts. Binding controls which local network interfaces accept traffic;
# it does not filter client source IPs. Use localhost for local-only access,
# and add a Tailscale hostname/IP when exposing through your tailnet.
SMART_HOME_BIND_HOSTS=127.0.0.1,your-tailnet-host.ts.net

# Tailscale hostname used in default CORS origins. Set to your tailnet node.
SMART_HOME_TAILSCALE_HOST=your-tailnet-host.ts.net

# Optional control token for mutating APIs. When set, physical actions accept
# X-Smart-Home-Token: <token> or Authorization: Bearer <token>.
SMART_HOME_API_TOKEN=replace-with-a-long-random-token

# Dedicated ESP-NOW bridge ingress. This path always fails closed when the
# token, principal, target mapping, or mode is missing. Keep dry_run until all
# idempotency and fault-injection tests pass; only the server may select live.
GARAGE_BRIDGE_TOKEN=replace-with-an-independent-long-random-token
GARAGE_BRIDGE_PRINCIPAL=garage-xiao-bridge-1
GARAGE_BRIDGE_DOOR_INDEX=1
GARAGE_BRIDGE_MODE=disabled

# Optional CORS allowlist for browser clients.
SMART_HOME_CORS_ORIGINS=http://localhost:7999,http://127.0.0.1:7999,http://your-tailnet-host.ts.net:7999,https://your-tailnet-host.ts.net

# Rinnai account
RINNAI_USERNAME=your_email@example.com
RINNAI_PASSWORD=your_password

# Meross account
MEROSS_EMAIL=your_email@example.com
MEROSS_PASSWORD=your_password
MEROSS_GARAGE_UUID=your-garage-controller-uuid
# MEROSS_GARAGE_NAME=Your Garage Controller Name
MEROSS_GARAGE_VERIFY_TIMEOUT_SECONDS=20
MEROSS_GARAGE_VERIFY_POLL_INTERVAL_SECONDS=2
MEROSS_GARAGE_CLOUD_TIMEOUT_SECONDS=10
# Refreshed after cloud discovery and used for LAN control during internet outages.
# Contains the Meross signing key; keep it under the gitignored data directory.
MEROSS_GARAGE_LOCAL_CACHE_PATH=data/meross_garage_local.json

# Optional: garage door trigger notification via Resend
# Enabled only when all four values are set and GARAGE_NOTIFY_ENABLED=true.
# GARAGE_NOTIFY_RECIPIENTS supports comma or semicolon separated addresses.
# RESEND_API_KEY may be a resolved key or an op:// 1Password secret reference.
GARAGE_NOTIFY_ENABLED=false
GARAGE_NOTIFY_RECIPIENTS=you@example.com;alerts@example.com
RESEND_API_KEY=op://your-vault/your-item/resend_api_key
RESEND_FROM_EMAIL="Smart Home <notifications@example.com>"

# Amcrest camera credentials
CAMERA_USER=your_camera_user
CAMERA_PASSWORD=your_camera_password

# Samsung TV via local Tizen WebSocket API (replaces SmartThings cloud PAT)
# The TV must be on the same LAN. First-run pairing: connect to wss://TV_IP:8002
# and approve the prompt on the TV. The local token is stored in
# data/samsung_tv_ws_token.txt (gitignored) and does not expire.
# Find the TV IP via mDNS: dns-sd -B _samsungmsf._tcp local.
SAMSUNG_TV_HOST=192.168.x.x
SAMSUNG_TV_PORT=8002
SAMSUNG_TV_APP_NAME=SmartHome
SAMSUNG_TV_TOKEN_FILE=data/samsung_tv_ws_token.txt
